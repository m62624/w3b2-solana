# W3B2 Frontend

React приложение для взаимодействия с W3B2 Bridge Protocol.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Настройка окружения
Создайте файл `.env` в корне проекта:
```env
REACT_APP_SOLANA_RPC_URL=https://api.devnet.solana.com
REACT_APP_PROGRAM_ID=W3B2Bridge111111111111111111111111111111111
REACT_APP_API_URL=http://localhost:3001/api
```

### Запуск в режиме разработки
```bash
npm start
```

Приложение будет доступно по адресу `http://localhost:3000`

### Сборка для продакшена
```bash
npm run build
```

## 🎨 Особенности UI

### Дизайн
- **Темная тема** - Современный темный интерфейс
- **Responsive** - Адаптивный дизайн для всех устройств
- **Анимации** - Плавные переходы и анимации
- **Иконки** - Lucide React иконки

### Компоненты
- **Layout** - Основной макет с боковой панелью
- **Dashboard** - Панель управления с статистикой
- **Wallet** - Управление кошельком и ключами
- **Funding** - Запросы на финансирование
- **Records** - CRUD операции с данными
- **Settings** - Настройки приложения

## 🔧 Функциональность

### Управление кошельком
- Создание нового кошелька
- Импорт существующего кошелька
- Экспорт кошелька в JSON
- Просмотр баланса
- История транзакций

### W3B2 Протокол
- Регистрация пользователя
- Запросы на финансирование
- CRUD операции с данными
- Управление сессиями
- Мониторинг статистики

### Безопасность
- Шифрование данных
- Безопасное хранение ключей
- Валидация входных данных
- Защита от XSS атак

## 📱 Страницы

### Dashboard (`/`)
- Обзор системы
- Статистика
- Быстрые действия
- Последние транзакции

### Wallet (`/wallet`)
- Управление кошельком
- Импорт/экспорт ключей
- Просмотр баланса
- История операций

### Funding (`/funding`)
- Запросы на финансирование
- Одобрение запросов
- Статистика финансирования

### Records (`/records`)
- CRUD операции
- Поиск и фильтрация
- Управление данными

### Settings (`/settings`)
- Настройки сети
- Конфигурация приложения
- Статистика системы
- Информация о версии

## 🎯 Хуки

### useWallet
Управление кошельком и Solana интеграцией:
```typescript
const {
  walletInfo,
  balance,
  initializeWallet,
  generateWallet,
  importWallet,
  disconnect,
  refreshBalance
} = useWallet();
```

### useApi
Работа с API сервером:
```typescript
const {
  registerUser,
  requestFunding,
  performCrudOperation,
  createSession,
  getStats
} = useApi();
```

## 🎨 Стилизация

### Tailwind CSS
- Утилитарные классы
- Кастомные цвета
- Адаптивный дизайн
- Темная тема

### Кастомные стили
- Glass эффекты
- Градиенты
- Анимации
- Кастомные компоненты

## 🔄 Состояние

### Контексты
- **WalletContext** - Состояние кошелька
- **ApiContext** - Состояние API

### Локальное состояние
- React hooks для локального состояния
- Формы с валидацией
- Модальные окна

## 📊 Интеграция

### Solana Web3.js
- Подключение к Solana сети
- Отправка транзакций
- Работа с кошельками
- Мониторинг событий

### API Integration
- Axios для HTTP запросов
- Обработка ошибок
- Автоматические retry
- Кэширование данных

## 🛠️ Разработка

### Структура проекта
```
src/
├── components/          # React компоненты
│   ├── Layout.tsx      # Основной макет
│   ├── Dashboard.tsx   # Панель управления
│   ├── Wallet.tsx      # Управление кошельком
│   ├── Funding.tsx     # Финансирование
│   ├── Records.tsx     # Записи данных
│   └── Settings.tsx    # Настройки
├── contexts/           # React контексты
│   ├── WalletContext.tsx
│   └── ApiContext.tsx
├── hooks/              # Кастомные хуки
│   ├── useWallet.ts
│   └── useApi.ts
├── services/           # Сервисы
│   ├── solanaService.ts
│   └── apiService.ts
├── types/              # TypeScript типы
│   └── index.ts
└── utils/              # Утилиты
```

### Добавление новых компонентов
1. Создайте компонент в `src/components/`
2. Добавьте типы в `src/types/`
3. Обновите роутинг в `App.tsx`
4. Добавьте навигацию в `Layout.tsx`

## 📝 Скрипты

- `npm start` - Запуск в режиме разработки
- `npm run build` - Сборка для продакшена
- `npm test` - Запуск тестов
- `npm run eject` - Извлечение конфигурации

## 🐛 Отладка

### React Developer Tools
Установите расширение для браузера:
- Chrome: React Developer Tools
- Firefox: React Developer Tools

### Console Logs
```javascript
// Включить подробные логи
localStorage.setItem('debug', 'w3b2:*');
```

### Network Tab
Проверьте Network tab в DevTools для отладки API запросов.

## 🚀 Деплой

### Build
```bash
npm run build
```

### Serve
```bash
# Используйте любой статический сервер
npx serve -s build
# или
npx http-server build
```

### Environment Variables
Убедитесь, что все переменные окружения настроены правильно для продакшена.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера
2. Убедитесь в правильности API URL
3. Проверьте подключение к Solana сети
4. Создайте issue в репозитории