# ===================================================================
# == Global Settings
# ===================================================================

# --- Versioning ---
# Defines the versions of key dependencies to be used in Docker builds.
SOLANA_VERSION=2.3.13
ANCHOR_VERSION=0.32.1

# --- Docker & Networking ---
# Ports for services exposed by docker-compose.
SOLANA_VALIDATOR_RPC_PORT_EXTERNAL=8899
SOLANA_VALIDATOR_WS_PORT_EXTERNAL=8900
SOLANA_VALIDATOR_FAUCET_PORT_EXTERNAL=8901
GATEWAY_PORT_EXTERNAL=50051
DOCS_PORT_EXTERNAL=8000

# Internal ports used inside the Docker network.
SOLANA_VALIDATOR_RPC_PORT_INTERNAL=8899
SOLANA_VALIDATOR_WS_PORT_INTERNAL=8900
SOLANA_VALIDATOR_FAUCET_PORT_INTERNAL=8901
GATEWAY_PORT_INTERNAL=50051
DOCS_PORT_INTERNAL=8000

# ===================================================================
# == Build & Deploy Settings
# ===================================================================

# --- Solana Program ---
# Base name for the Solana program. Used to derive keypair, IDL, and .so filenames.
PROGRAM_NAME=w3b2_solana_program

# Directory name for the on-chain program. Often uses hyphens.
PROGRAM_DIR_NAME=w3b2-solana-program

# Filename for the program's keypair, which also serves as its on-chain address (Program ID).
PROGRAM_KEYPAIR_FILENAME=${PROGRAM_NAME}.keypair.json
PROGRAM_KEYPAIR_PATH=/keys/${PROGRAM_KEYPAIR_FILENAME}

# Path inside the Docker container to the deployer's keypair file.
# This wallet is used to pay for the transaction fees when deploying the program.
# The deploy script will create it if it doesn't exist.
DEPLOYER_KEYPAIR_FILENAME=deployer-keypair.json
DEPLOYER_KEYPAIR_PATH=/keys/${DEPLOYER_KEYPAIR_FILENAME}

# --- File Paths ---
GATEWAY_CONFIG_PATH=config.docker.toml
# Filename for the program's IDL (Interface Definition Language).
PROGRAM_IDL_FILENAME=${PROGRAM_NAME}.idl.json
# Filename for the compiled on-chain program binary.
PROGRAM_SO_FILENAME=${PROGRAM_NAME}.so